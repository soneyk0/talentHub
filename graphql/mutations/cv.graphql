mutation CreateCV($cv: CreateCvInput!) {
    createCv(cv: $cv) {
        id
        name
        education
        description
    }
}

mutation DeleteCV($cv: DeleteCvInput!) {
    deleteCv(cv: $cv){
        affected
    }
}